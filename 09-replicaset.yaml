apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-rs
  labels:
    purpose: rs-demo
    project: roboshop
    environment: dev
spec:
  replicas: 3
  selector:
    matchLabels:
      purpose: rs-demo
      project: roboshop
      environment: dev
  template:
    metadata:
      labels:
        purpose: rs-demo
        project: roboshop
        environment: dev
    spec:
      containers:
      - name: nginx
        image: nginx